{
  "name": "server",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.1.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.5.4"
  }
}
